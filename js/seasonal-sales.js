"use strict";
// access a product category id with 
// product.category_id === category.id
// ^ product json          ^ categories json

// Practice examples:
// let lightbulb = null;
// categories.forEach(cat => {
//     if (cat.id === 3){
//         lightbulb.category = cat.name;
//     }
// });

/* TO DO LIST:
- html page
    - list of products
    - select element
- json files
    - products
    - categories
- js files
    - formatting the data
    - xhrs, something to interact with the data
    - display the data, and output to the DOM
    - DOM interactions, with user input
    - main js file that calls the other modules (controller)


*/